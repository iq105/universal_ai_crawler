# AGENT.md —— Python 大模型 Agent 开发规范

> **必选 20 条**：所有项目必须遵守，违反任一条视为不合格。
> **可选 30 条**：根据项目阶段和复杂度逐步启用，建议生产环境全部遵守。

---

# 一、必选 20 条（红线）

## 1. LLM 调用统一封装
所有 LLM 调用必须通过 `llm_client` 模块，禁止业务代码直接 `import openai` 或 `ChatOpenAI`。
必须区分**流式**与**非流式**接口，统一处理 API Key、超时、重试、计费、Token 统计。

## 2. 禁止裸 `except` 和 `pass`
所有异常必须捕获并记录日志，禁止 `except: pass`。
必须区分：可重试异常、业务异常、系统异常。

## 3. 系统异常必须脱敏
对外禁止暴露堆栈、SQL、内部 IP、API Key、模型名称。
控制台可以显示堆栈、SQL、内部 IP、模型名称，其他敏感信息不可以打印在控制台里面
## 3。1. 对外接口返回错误数据规范
对外接口必须模糊提示，禁止暴露堆栈、SQL、内部 IP、API Key、模型名称、用户名、密码、银行卡等敏感信息
统一提示：“系统繁忙，请稍后重试。如持续出现，请联系管理员。” 
## 3。2. 代码功能必须分层
系统必须分层，分成对外接口api，内部服务，数据库(数据接入)这3层，每层代码各司其职，只能上层调用下层，禁止下层调用上层
每层代码只做自己的职责，不可越权

## 4. 用户提示必须人性化
所有面向用户的提示必须说明**发生了什么**和**可以怎么做**，禁止只返回错误码。
❌ `{"error": "NullPointerException"}`
✅ `"您提交的订单中商品已下架，请刷新后重试。"`

## 5. 日志必须携带 trace_id
每个请求必须有唯一 `trace_id`，贯穿 API → Agent → LLM → 工具 → 数据库。
日志格式：`时间戳 | 级别 | 模块 | trace_id | 消息`。

## 6. 函数不超过 100 行
超过必须拆分。单个文件不超过 500 行，超过按职责拆分模块。

## 6.1. 函数命名与注释
函数命名必须有业务意义，否则看不懂，
函数必须写注释，否则不知道函数的作用,不写注释标记为没写完代码

## 6.2. 业务承接
函数命名必须有业务意义，否则看不懂，
函数必须写注释，否则不知道函数的作用,不写注释标记为没写完代码

## 7. 职责单一
一个函数只做一件事。禁止在同一个函数里既调 LLM、又写数据库、又发消息。

## 8. 禁止魔法值
所有常量（模型名、超时、重试次数、表名）必须定义在 `config.py` 或 `constants.py`。

## 9. 数据库统一 ORM
必须使用 **SQLAlchemy 2.0（异步）**，禁止混用 `pymysql`、`aiomysql`、原生 SQL。
所有数据库操作封装在 `repositories/` 层。

## 10. 连接池必须配置
必须显式配置连接池大小、超时、回收策略。禁止每次请求新建连接。

## 11. State 必须用 TypedDict 明确定义
禁止用 `dict` 随意传参。并行写入字段必须使用 `Annotated[list, operator.add]` 等 reducer。

## 12. Checkpointer 生产环境必须持久化
开发环境可用 `MemorySaver`，生产必须用 `PostgresSaver` 或 `SqliteSaver`。
每次调用必须传 `thread_id`。

## 13. 工具异常必须结构化返回
工具内部异常必须通过 `Command(update={"error": ...})` 返回，禁止直接抛出异常让图崩溃。
工具返回消息必须包含 `tool_call_id`，禁止产生“孤儿 tool_calls”。

## 14. 配置外置
API Key、数据库密码、模型地址必须从环境变量或 `.env` 读取，禁止硬编码。
必须提供 `.env.example` 模板。

## 15. 输入必须校验
所有用户输入必须校验长度、类型、敏感词，禁止直接拼接进 Prompt 或 SQL。
防注入、防 SSRF（禁止访问 `localhost`、`169.254.169.254` 等内网地址）。

## 16. 异步禁止阻塞事件循环
禁止在异步框架中使用 `time.sleep()`、`requests.get()`。
必须使用 `asyncio.sleep()`、`httpx.AsyncClient`，或用 `asyncio.to_thread()` 包装同步库。

## 17. 并发必须有上限
调用 LLM、外部 API、数据库必须使用 `asyncio.Semaphore` 或连接池限流。
禁止无限制 `asyncio.gather()`，必须分批或限流，并设置超时。

## 18. 上下文长度必须控制
发送给 LLM 的消息历史必须截断或摘要，禁止无限增长。
必须设置最大 Token 上限，超出时按策略裁剪。

## 19. 敏感信息必须脱敏
用户输入中的手机号、身份证、银行卡在进入 LLM 前必须脱敏。
LLM 返回内容含敏感信息时，输出前必须过滤。

## 20. AI 生成代码必须自检
生成代码后，AI 必须自检并输出结果：
- [ ] 有没有裸 `except`？
- [ ] 有没有直接调用 LLM？
- [ ] 函数是否超过 100 行？
- [ ] 用户提示是否人性化？
- [ ] 是否使用了统一 ORM？
- [ ] 是否有日志和 trace_id？

---

# 二、可选 30 条（建议生产环境启用）

## 21. 结构化输出优先
优先使用 `with_structured_output` 或 `PydanticOutputParser`。
若模型不支持 `response_format`，必须自动降级为 `PydanticOutputParser`。

## 22. LLM 调用可观测
每次调用记录 `model`、`input_tokens`、`output_tokens`、`latency_ms`、`cost`。
日志中禁止打印完整 Prompt 和用户隐私数据。

## 23. 异常分级重试
可重试异常（超时、429、5xx）指数退避，最多 3 次。
业务异常直接返回用户提示，系统异常隐藏细节。

## 24. 表结构变更用 Alembic
禁止手动 `CREATE TABLE`，必须通过 Alembic 迁移。

## 25. 会话严格隔离
不同用户通过 `thread_id` 隔离，禁止跨用户共享状态。
`thread_id` 必须与业务身份绑定，禁止随机生成后丢失。

## 26. Prompt 与代码分离
所有 Prompt 存放在 `prompts/` 目录，禁止硬编码。
Prompt 必须版本化管理。

## 27. Prompt 注入防护
用户输入必须与系统 Prompt 明确分隔，禁止直接拼接。
必须对用户输入做转义或沙箱处理。

## 28. 工具必须有超时和降级
每个工具调用设置超时，超时后返回明确错误。
外部服务不可用时，必须有降级策略（缓存、默认值、跳过）。

## 29. 工具幂等性
有副作用的工具（写库、发消息、扣款）必须保证幂等。
必须使用唯一业务 ID 去重。

## 30. 第三方 SDK 必须封装
所有第三方 SDK（支付、短信、地图）封装在 `integrations/` 层，禁止业务代码直接调用。

## 31. 依赖版本锁定
必须提供 `requirements.txt` 或 `pyproject.toml`，锁定具体版本。
禁止使用 `latest` 或不指定版本。

## 32. Python 版本统一
项目必须明确 Python 版本（如 3.11+），并在 `pyproject.toml` 中声明。

## 33. 避免 N+1 查询
数据库查询必须使用 `JOIN` 或批量查询，禁止循环中逐条查询。
必须使用 `selectinload` 或 `joinedload` 预加载关联数据。

## 34. 大对象流式处理
大文件、大 JSON 必须流式处理，禁止一次性加载到内存。
必须设置内存上限，防止 OOM。

## 35. 缓存策略明确
缓存必须有明确过期时间和失效策略。
缓存 Key 必须包含版本号，便于批量失效。

## 36. 权限校验
每个 API 接口必须校验用户权限，禁止仅依赖前端隐藏。
必须遵循最小权限原则，禁止使用超级管理员运行业务。

## 37. 审计日志
敏感操作（删除、修改权限、支付）必须记录审计日志。
审计日志必须包含：操作人、时间、操作内容、结果。

## 38. 密钥轮换
API Key、数据库密码必须支持轮换，禁止永久有效。
密钥必须加密存储，禁止明文写在配置文件。

## 39. 健康检查
每个服务必须提供 `/health` 接口，返回依赖服务状态。
必须区分 `liveness` 和 `readiness`。

## 40. 优雅关闭
服务必须支持优雅关闭：停止接收新请求、等待进行中任务完成、释放资源。
必须处理 `SIGTERM` 信号。

## 41. 配置热更新
关键配置（限流阈值、开关）必须支持热更新，禁止重启才能生效。

## 42. 生成前先读规范
AI 生成代码前，必须先阅读并确认遵守本 `AGENT.md`。

## 43. 禁止臆造 API
AI 禁止使用不存在的库、函数或参数。
不确定时必须明确说明并请求确认。

## 44. 增量修改，禁止大范围重写
修改代码时最小化改动范围，禁止无关重构。
必须保留原有注释和文档，除非明确要求删除。

## 45. 必须提供完整可运行代码
禁止输出 `...` 省略号或 `# 此处省略` 等占位符。
所有导入、依赖、配置必须完整。

## 46. 公共函数必须有 docstring
所有公共函数、类、模块必须有 docstring，说明用途、参数、返回值、异常。
复杂逻辑必须有行内注释，解释“为什么”而非“做什么”。

## 47. README 必须更新
新增功能或修改架构时，必须同步更新 `README.md`。
必须包含：快速开始、环境变量说明、常见问题。

## 48. 提交信息规范
Git commit 必须遵循 `type(scope): message` 格式。
禁止提交无意义的 `update`、`fix`。

## 49. 变更必须可回滚
每次变更必须保证可回滚，禁止不可逆操作。
数据库变更必须提供 `upgrade` 和 `downgrade`。

## 50. 单元测试必须覆盖核心逻辑
核心逻辑（LLM 封装、工具、状态机）必须有单元测试，Mock 掉外部依赖。
单元测试代码保存在test目录下面
禁止提交没有测试的核心功能。

##51 .约束：若要引入 Agent，只能用 LangChain/LangGraph 生态的 create_agent，不得用其它 agent 框架
---

**一句话总结**：**LLM 统一封装、异常必须处理、用户提示人性化、代码模块化、数据库统一 ORM、Agent 状态可控、异步不阻塞、并发有上限、安全有审计、生成可自检。**